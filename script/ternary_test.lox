// var b = true;
//  var a = b ? "yes" : "no";
// var c = a == 1 ? 1 >= 3*8 : true or false;

// true ? "yes" : "no";
// 0 == 1 ? 1 >= 3*8 : true == false;

// 0 == 1 ? 1 >= 3*8 : true ? "a" : "b";
// => 0 == 1 ? 1 >= 3*8 : (true ? "a" : "b");

0 == 1 ? false ? "hey" : "ho" : true ? "a" : "b";
// => 0 == 1 ? (false ? "hey" : "ho") : (true ? "a" : "b");

//true ? 2 : 3 ? false ? "hey" : "ho" : true ? "a" : "b";
// => (true ? 2 : 3) ? (false ? "hey" : "ho") : (true ? "a" : "b");